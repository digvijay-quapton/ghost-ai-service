apiVersion: v1
kind: Service
metadata:
  name: ghost-ai-service
  namespace: ghost-ai
  labels:
    app: ghost-ai-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: ghost-ai-service